# Nothing here is required any more.
#
# extract/extract.py was the only step that used a key, and the transcription it
# produced is committed. Further work on the archive goes through Claude Code on the
# subscription; the site build and the derive step run offline.
#
# ANTHROPIC_API_KEY=sk-ant-...   # only if you deliberately re-run extract.py
